import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  images: {
    domains: [], // Add your image domains here if images are hosted externally
  },
  /* config options here */
};

export default nextConfig;
